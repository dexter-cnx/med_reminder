# Android native handoff

The Flutter baseline uses `flutter_local_notifications` directly. Native ongoing-notification and Wear OS Data Layer integration should be implemented after `tool/bootstrap_platforms.sh` creates the Android host project. Keep the channel names `med_reminder/live_activity` and `med_reminder/watch_sync` aligned with Dart.
